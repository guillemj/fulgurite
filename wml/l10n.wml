<:
my %langs_map = (
  ca => { Language => 'Catalan',
	  Charset  => 'utf-8',
	},
  es => { Language => 'Spanish',
	  Charset  => 'utf-8',
	},
  en => { Language => 'English',
	  Charset  => 'utf-8',
	},
);

my $LANGUAGE;
my $CHARSET;

if (defined(my $conf = $langs_map{$LANG})) {
  $LANGUAGE = $conf->{'Language'};
  $CHARSET = $conf->{'Charset'};
}
:>

